var request = require('request');
var expect = require('chai').expect;

describe('Games', function() {
  it("should post a new game", function(done) {
  });

  it("should get an existing game's summary", function(done) {
  });

  it("should get a list of games played by a specific duo", function(done) {
  });

  it("should post a comment for a specific game", function(done) {
  });

  it("should get a list of comments for a specific game", function(done) {
  });
});
